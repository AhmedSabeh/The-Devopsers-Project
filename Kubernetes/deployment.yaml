apiVersion: apps/v1
kind: Deployment
metadata:
  name: devopsers
  namespace: devopsers
  labels:
    app: devopsers
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devopsers
  template:
    metadata:
      labels:
        app: devopsers
    spec:
      containers:
        - name: devopsers
          image: ahmedsabeh/devopsers:1.14
          ports:
            - containerPort: 80
